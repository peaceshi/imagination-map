import React, { ReactElement } from "react";

import MapLoader from "./MapLoader";

const App = (): ReactElement => {
  return (
    <div className="App">
      <MapLoader />
    </div>
  );
};
export default App;
